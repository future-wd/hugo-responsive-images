DURING resize

2. check image width
range through widths 
3. if image width less than current width
 >  skip width, flag removal

IF FLAG 
 > check if image width is DIFFERENCE greater than largest resize width
 IF YES >
  resize to this width
5. Generate warning, print the largest width that was generated

DURING PARAMS 
range through $widths
only add width to new array if if image width ge than current width
if not flag

if flag check if last width in array is 
